#:import Utils library.utils.Utils

<PriceLabel@Label>:
    btcprice: 0
    btcprice_time: ''

    font_size: 50
    text: '{} {:,} BTC{}[size={}]\n ({}) [/size]'.format(app.fiat.mark, app.fiat.cent_to_dollar(root.btcprice), app.fiat.name, int(self.font_size/2), self.btcprice_time)
    halign: 'right'
    markup: True
